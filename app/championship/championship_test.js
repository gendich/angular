'use strict';

describe('myApp.championship module', function() {

    beforeEach(module('myApp.championship'));

    describe('championship controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var ChampCtrl = $controller('ChampionshipCtrl');
            expect(ChampCtrl).toBeDefined();
        }));

    });
});